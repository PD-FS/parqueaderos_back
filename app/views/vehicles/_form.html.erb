<%= form_with(model: vehicle, local: true) do |form| %>
  <% if vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :license %>
    <%= form.text_field :license, id: :vehicle_license %>
  </div>

  <div class="field">
    <%= form.label :vehicleTypeId_id %>
    <%= form.text_field :vehicleTypeId_id, id: :vehicle_vehicleTypeId_id %>
  </div>

  <div class="field">
    <%= form.label :userId_id %>
    <%= form.text_field :userId_id, id: :vehicle_userId_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
