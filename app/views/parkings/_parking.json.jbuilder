json.extract! parking, :id, :name, :typeDocument, :document, :address, :userId_id, :phone, :movil, :email, :latitude, :longitude, :serviceDays, :openingTime, :closingTime, :created_at, :updated_at
json.url parking_url(parking, format: :json)
