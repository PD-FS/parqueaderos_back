<%= form_with(model: parking_type, local: true) do |form| %>
  <% if parking_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parking_type.errors.count, "error") %> prohibited this parking_type from being saved:</h2>

      <ul>
      <% parking_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :parkingId_id %>
    <%= form.text_field :parkingId_id, id: :parking_type_parkingId_id %>
  </div>

  <div class="field">
    <%= form.label :vehicleTypeId_id %>
    <%= form.text_field :vehicleTypeId_id, id: :parking_type_vehicleTypeId_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :parking_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
