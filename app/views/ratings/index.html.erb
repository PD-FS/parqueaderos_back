<p id="notice"><%= notice %></p>

<h1>Ratings</h1>

<table>
  <thead>
    <tr>
      <th>Parking</th>
      <th>Parkingid</th>
      <th>Userid</th>
      <th>Date</th>
      <th>Score</th>
      <th>Observations</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ratings.each do |rating| %>
      <tr>
        <td><%= rating.parking %></td>
        <td><%= rating.parkingId %></td>
        <td><%= rating.userId %></td>
        <td><%= rating.date %></td>
        <td><%= rating.score %></td>
        <td><%= rating.observations %></td>
        <td><%= link_to 'Show', rating %></td>
        <td><%= link_to 'Edit', edit_rating_path(rating) %></td>
        <td><%= link_to 'Destroy', rating, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rating', new_rating_path %>
